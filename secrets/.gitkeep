Put a Netscape-format `cookies.txt` here (exported from a logged-in browser,
e.g. via the "Get cookies.txt LOCALLY" extension) and set in .env:

    YT_DLP_COOKIES_FILE=/secrets/cookies.txt

Then restart the worker:  docker compose up -d --force-recreate worker
